<div class="container">
  <h1>Game n°<%= @game.id %></h1>
  <div class="grid">
      <% @names.each do |name| %>
        <div class="cards" data-controller="return-card" data-return-card-target="cards" data-action="click->return-card#return">
          <%= name.upcase %>
        </div>
      <% end  %>
   </div>
</div>
